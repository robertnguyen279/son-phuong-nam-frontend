import React from 'react';

const Loading = (): React.ReactElement => {
  return (
    <div className="loading">
      <span className="loading__meter"></span>
    </div>
  );
};

export default Loading;
